install python latest version
(click set path to environment variable without fail)

open command prompt
type
pip install sklearn
pip install pandas
pip install opencv-python
pip install matplotlib
pip install numpy


now copy the project folder to local disk D(or change your path name in img_process.py file);


change your name in setup.py file


open images training folder run image.py and train your leaf.



step 1:open testimage folder and run image.py
press spacebar which take screenshot of image,and save in same folder.

step 2:run img_process.py 
 

it will give the output
of diseased portion
